import PropTypes from 'prop-types'

const Profile = ({ name, work, pic }) => {
    return (
       <img src=''
    )

}

Profile.propTypes = {
    name: PropTypes.string.isRequired,
    work: PropTypes.string.isRequired,
    Pic:  
    work: PropTypes.string.isRequired,

}

export default Profile